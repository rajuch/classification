apt-get -y install munin-node
debian_frontend=noninteractive apt-get -y install -qq memcached python-cheetah python-memcache
version
problems with: %s
s [-h host] [-p port] -c check_type\n\n
for options, run
h --hosts string
hosts comma separated list of hosts to check - defaults to
p --ports string
ports comma separated list of ports to check - defaults to 11211
c --check-type string
check_type the type of check - mandatory!
localhost
11211
you haven
s:%s
you must specify a type of check to perform
version
check types currently accepted: version
